import React from 'react';
import fakeData from '../../fakeData'
const Shop = () => {
    console.log(fakeData);
    return (
        <div>
            Hello
        </div>
    );
};

export default Shop;